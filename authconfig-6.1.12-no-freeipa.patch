diff -up authconfig-6.1.12/authconfig-gtk.py.no-freeipa authconfig-6.1.12/authconfig-gtk.py
--- authconfig-6.1.12/authconfig-gtk.py.no-freeipa	2010-12-23 17:38:53.000000000 +0100
+++ authconfig-6.1.12/authconfig-gtk.py	2011-01-13 17:24:50.000000000 +0100
@@ -99,9 +99,9 @@ class Authconfig:
 			"LDAP":
 			(_("LDAP"), ("Kerberos", "LDAPAuth"),
 			 "ldapoptions", "ldap_map", (authinfo.PATH_LIBNSS_LDAP, authinfo.PATH_LIBNSS_SSS), ("nss-pam-ldapd", "sssd-client")),
-			"FreeIPA":
-			(_("FreeIPA"), ("Kerberos",),
-			 "ldapoptions", "ldap_map", (authinfo.PATH_LIBNSS_LDAP, authinfo.PATH_LIBNSS_SSS), ("nss-pam-ldapd", "sssd-client")),
+#			"FreeIPA":
+#			(_("FreeIPA"), ("Kerberos",),
+#			 "ldapoptions", "ldap_map", (authinfo.PATH_LIBNSS_LDAP, authinfo.PATH_LIBNSS_SSS), ("nss-pam-ldapd", "sssd-client")),
 			"NIS":
 			(_("NIS"), ("NISAuth", "Kerberos"),
 			 "nisoptions", "nis_map", authinfo.PATH_LIBNSS_NIS, "ypbind"),
@@ -110,7 +110,8 @@ class Authconfig:
 			 "winbindoptions", "winbind_map", authinfo.PATH_WINBIND, "samba-winbind")
 		}
 		# to keep the order we need a tuple
-		self.id_keys = ("local", "LDAP", "FreeIPA", "NIS", "Winbind")
+		self.id_keys = ("local", "LDAP",# "FreeIPA",
+				"NIS", "Winbind")
 		# "auth type": localized name,
 		# option widget, option map name, file, package
 		self.auth_map = {
@@ -367,8 +368,8 @@ class Authconfig:
 			if meth != 'local':
 				try:
 					if getattr(self.info, 'enable' + meth):
-						if meth == 'LDAP' and self.info.ldapSchema == 'rfc2307bis':
-							meth = 'FreeIPA'
+#						if meth == 'LDAP' and self.info.ldapSchema == 'rfc2307bis':
+#							meth = 'FreeIPA'
 						return meth
 				except AttributeError:
 					pass
